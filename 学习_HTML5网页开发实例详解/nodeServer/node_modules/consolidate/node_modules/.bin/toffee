#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../toffee/bin/toffee" "$@"
  ret=$?
else 
  node  "$basedir/../toffee/bin/toffee" "$@"
  ret=$?
fi
exit $ret
